import { FiltroPlantasPipe } from './filtro-plantas-pipe';

describe('FiltroPlantasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPlantasPipe();
    expect(pipe).toBeTruthy();
  });
});
